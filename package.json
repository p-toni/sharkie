{
  "name": "sharkie-mascot",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "A tiny animated SVG shark mascot for the web.",
  "license": "MIT",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "test": "vitest run"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "vite": "^7.1.0",
    "vitest": "^3.2.4"
  }
}
